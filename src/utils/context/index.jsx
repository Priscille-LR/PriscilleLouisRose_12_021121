// import { createContext, useState } from 'react';
// import { dependencies } from '../../service/Dependencies';

/**
 * Create context to switch data sources :
 * data comes either from the mocks or the API
 */

// export const SourceContext = createContext();

// export const SourceProvider = ({ children }) => {
//    const [dependencies] = useState('api');
//    // const switchSource = () => {
//    //    setSource(source === 'mock' ? 'api' : 'mock');
//    // };

//    return (
//       <SourceContext.Provider value={{ dependencies }}>
//          {children}
//       </SourceContext.Provider>
//    );
// };
